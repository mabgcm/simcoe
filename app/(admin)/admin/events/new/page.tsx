import { ContentForm } from "@/components/admin/content-form";

/** Admin create event page. */
export default function NewEventPage() {
  return <ContentForm title="Yeni İçerik" defaultType="event" />;
}
